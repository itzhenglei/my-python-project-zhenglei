---
description: 本仓库 AI 协作、Python 习惯、Git 与测试自动化相关约定（始终应用）
alwaysApply: true
---

# 协作与编码约定（Cursor 规则）

更完整的习惯知识库见仓库根目录 **`AGENTS.md`**。

## 语言与解释

- 使用**简体中文**回复。
- 复杂逻辑在对话中讲清楚；源码注释「能让人看懂即可」，不要求每行 `#`。
- **快乐 8 邮件 HTML**：大段 HTML/CSS 的 f-string 内**不要**写 `#` 注释（会破坏邮件或 Python 语法）。
- 涉及行为变化（如排序）时，先**方案对比**再实现；当前号序采用**方案 A**（主流程先取胆再 `sorted`，邮件/JSON/打印一致）。

## 导入习惯

- **工程内模块**（如 `utils.common_page`、`page.xxx`）：为避免循环导入，可在**函数内**或 **`__init__`** 中按需导入。
- **非工程内模块**（标准库如 `logging`、`re`，第三方如 `loguru`、`appium` 等）：在**文件顶部**导入即可。

## 文件与配置习惯

- **`pytest.ini`**：集中配置 `addopts`、`markers`，并与 **allure** 报告输出目录配合。
- **`config.json`**：集中管理远端服务器、ipa 路径等环境配置。
- **文件名**：使用有意义的**英文**（如 `test_login.py`、`base_page.py`、`log_keyword.py`），**不用中文文件名**。

## 对助手的文件操作

- **新建文件**：可直接创建，无需事先征得同意。
- **修改或删除**已有文件：必须事先说明范围与方案，**征得同意**后再执行。
- **重构/批量改动前**：先生成**临时文件**（如 `xxx_TEMP.py`），内容为**待改动后的完整文件**，用户确认后再覆盖原文件；**改动完成后删除**临时文件。
- 可在适当时机**新建辅助类/说明文件**以增强协作（如习惯说明、规则）。

## Git 习惯

- **任何改动**（代码、配置等）：先说明方案，**用户确认后再执行**。
- **恢复/撤销**：`git reset`、`git revert`、回退等同样需先说明并征得同意。
- **提交流程**：说明将执行的命令及作用；列出涉及文件；区分 **AI 完成** 与 **人为编写**；commit message 中写明 AI 完成部分；**用户确认后**再 `git commit`；**`git push` 前单独征求同意**。
- 仅提交约定文件；不要把本地预测产物等随意加入版本库，除非用户要求（参见 `AGENTS.md` 与项目文档）。

## 主动行为与知识库

- 从用户表述中**主动提取**习惯与偏好，补充到 **`AGENTS.md`**，不必等用户反复提醒。
- 每次对 **`AGENTS.md`** 有**新增或修改**习惯条目时，在**当次回复中明确告知**用户（例如「已记录到 AGENTS.md」）。
- 在能力范围内**主动完成可自动完成的事项**，减少重复指令。

## 测试与代码质量

- **方法治理**：新增方法若验证无效且未被引用，应及时删除；若仅当前场景使用且实现错误，在确认不影响其他代码后可就地修正，不保留无效试错方法。

## 工具与环境（参考）

- 常用 **PyCharm**；若命令行 Git 403 而 IDE 正常，多与 HTTPS PAT / 凭据有关；`credential.helper` 建议与系统钥匙串等与 IDE 一致。
