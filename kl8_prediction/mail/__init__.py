"""
邮件子包：`content` 生成 HTML，`sender` 发信，`stats`/`grid` 为邮件专用的统计与表格片段。

导入示例（一般不必对外 re-export）::

    from kl8_prediction.mail.content import generate_email_content
    from kl8_prediction.mail.sender import send_email
"""
